# Required: OpenAI API key for arena generation
OPENAI_API_KEY=sk-your-key-here

# Optional: Rate limit per IP per hour (default: 10)
ARENA_RATE_LIMIT_PER_IP=10

# Optional: App URL for share links (auto-detected on Vercel)
NEXT_PUBLIC_APP_URL=https://your-app.vercel.app
